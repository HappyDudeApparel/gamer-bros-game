# Prism Kit — Coordination Log (Claude ⇄ ChatGPT)

A shared, durable channel for whichever AI session (Claude Code or
ChatGPT) is currently driving Prism Kit work to leave the other a
compact, factual status update — so neither has to re-derive state from
scratch or from a pasted conversation transcript.

**Note on this file's history:** it did not exist anywhere in this
repository before the entry below — checked across all branches
(`main`, `pass18-rebuild`, `pass18-prism-kit`) and full git history, not
assumed. This is genuinely the first entry, not a continuation of a
prior log that was lost.

Newest entry at the top. Each entry should be short: what changed, what's
verified, what's next — not a narrative.

---

## Latest: Claude -> ChatGPT (this entry)

- **Branch / HEAD:** `pass18-prism-kit`, at `434c74b9ca` as of writing
  this entry (this entry itself lands in the next commit on top of that).
- **Read status:** Read `PRISM_KIT_HANDOFF.md`, `PRISM_KIT_RULES.md`,
  `PRISM_KIT_PLAN.md`, `START_NEXT_CLAUDE.md` this session (not re-reading
  Pass 17/18 history — those files are the authoritative summary). Will
  use this file as the shared coordination channel going forward: reading
  it at the start of a Prism Kit session and leaving an entry at natural
  stopping points.
- **Images:** confirmed I can visually see both attached concept images
  in this session — the "PRISM VALLEY V2" asset/style sheet (sections
  1-6: Terrain, Riverworks, Ruins, Nature, Gameplay, Portal Shrine) and
  the labeled A-F world map (Portal Meadow -> Creek Crossing ->
  Riverworks -> Clover Cliffs -> Ruin Courtyard -> Prism Ridge). Per the
  user's instruction, the raw image files are **not** being committed to
  this public repository — they're being used for in-session visual
  analysis only, with observations captured as text/measurements in
  `references/prism-valley/m1-overhang-reference.json` instead of as
  binary image files.
- **Toolchain correction found and fixed this session (worth knowing
  about if you touch the Node pipeline):** the vendored
  `vendor/three/addons/exporters/GLTFExporter.js` was originally fetched
  matching three.js r123, but the actual vendored core
  (`vendor/three/three.module.min.js`) is r180 — a real version mismatch,
  caught by actually running an import+export smoke test in Node rather
  than trusting file presence. Replaced with the r180-matched exporter.
  Also discovered the exporter needs a couple of browser-only globals
  (`FileReader`) that don't exist in Node; added a narrow polyfill at
  `tools/prism-kit/lib/node-three-env.mjs` plus a
  `node_modules/three` re-export shim (gitignored, regenerated by
  `npm run prism:doctor`) so Node scripts can `import ... from 'three'`
  exactly like the browser code does. `npm run prism:doctor` now
  actually exercises `GLTFExporter.parse()` end-to-end rather than just
  checking the file exists.
- **Immediate next action:** proceed into M1 Round 1 — analyze the
  Overhang icon (Terrain section, row 2 column 2 of the V2 sheet) against
  the checklist in the task, build its geometry procedurally in
  Three.js/Node using the now-verified-working export path, run it
  through the full pipeline to `VISUAL-REVIEW-STOP`, and score it against
  the reference.
