import * as THREE from 'three';
import {createGamerBro} from '../playground-v2/gamer-bro.js?v=pass9';
import {createCrystalLibraryTubeV2} from '../src/crystal-library-tube-v2.js?v=1';

const mobile=/Android|iPhone|iPad|iPod/i.test(navigator.userAgent)||matchMedia('(pointer:coarse)').matches;
const qp=new URLSearchParams(location.search),requested=qp.get('hero'),saved=localStorage.getItem('gamerBroCharacter');
const id=requested==='gb1'||requested==='gb2'?requested:(saved==='gb1'||saved==='gb2'?saved:'gb2'),colorway=id==='gb1'?'pink':'teal';localStorage.setItem('gamerBroCharacter',id);
const $=s=>document.querySelector(s),canvas=$('#game'),heroName=$('#heroName'),menu=$('#menu'),objective=$('#objective'),pad=$('#pad'),stick=$('#stick'),jumpBtn=$('#jump'),runBtn=$('#run'),actionBtn=$('#action'),retry=$('#retry'),toastEl=$('#toast'),boot=$('#boot'),status=$('#status'),bar=$('#bar'),fatal=$('#fatal'),errorEl=$('#error');heroName.textContent=id.toUpperCase();menu.onclick=()=>location.href='../';
function progress(t,n){status.textContent=t;bar.style.width=n+'%'}function toast(t,ms=1500){toastEl.textContent=t;toastEl.classList.add('show');clearTimeout(toast._t);toast._t=setTimeout(()=>toastEl.classList.remove('show'),ms)}

const renderer=new THREE.WebGLRenderer({canvas,antialias:true,alpha:false,powerPreference:'high-performance'});renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.AgXToneMapping;renderer.toneMappingExposure=1.38;renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.setPixelRatio(Math.min(devicePixelRatio||1,mobile?1:1.35));
const scene=new THREE.Scene();scene.background=new THREE.Color(0x8fcce3);scene.fog=new THREE.Fog(0xa3d1df,52,118);const camera=new THREE.PerspectiveCamera(36,1,.15,160);camera.layers.enable(1);scene.add(new THREE.HemisphereLight(0xebfbff,0x456044,1.85));const sun=new THREE.DirectionalLight(0xffefd4,2.2);sun.position.set(-28,38,18);sun.castShadow=true;sun.shadow.mapSize.set(mobile?1024:1536,mobile?1024:1536);sun.shadow.camera.left=-55;sun.shadow.camera.right=55;sun.shadow.camera.top=55;sun.shadow.camera.bottom=-55;sun.shadow.camera.near=1;sun.shadow.camera.far=110;sun.shadow.bias=-.00025;sun.shadow.normalBias=.03;scene.add(sun);const rim=new THREE.DirectionalLight(0x8d71ff,.28);rim.position.set(25,18,-24);scene.add(rim);
const world=new THREE.Group();scene.add(world);const ray=new THREE.Raycaster(),down=new THREE.Vector3(0,-1,0),walkMeshes=[];let worldBounds=new THREE.Box3();
const mats={grass:new THREE.MeshStandardMaterial({color:0x70a94e,roughness:.92}),grass2:new THREE.MeshStandardMaterial({color:0x5f9344,roughness:.94}),path:new THREE.MeshStandardMaterial({color:0xc6a979,roughness:.98}),stone:new THREE.MeshStandardMaterial({color:0x777a72,roughness:.9}),stone2:new THREE.MeshStandardMaterial({color:0x97998d,roughness:.9}),wood:new THREE.MeshStandardMaterial({color:0x7a4b2d,roughness:.88}),water:new THREE.MeshPhysicalMaterial({color:0x2f9abe,roughness:.14,metalness:.02,transmission:.22,transparent:true,opacity:.78}),roof:new THREE.MeshStandardMaterial({color:0x8f4d3c,roughness:.86}),plaster:new THREE.MeshStandardMaterial({color:0xe4d5ad,roughness:.95}),leaf:new THREE.MeshStandardMaterial({color:0x4f8c47,roughness:1})};
function box(name,x,y,z,w,h,d,mat,walk=false,ry=0,rx=0){const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),mat);m.name=name;m.position.set(x,y,z);m.rotation.set(rx,ry,0);m.castShadow=!mobile;m.receiveShadow=true;world.add(m);if(walk)walkMeshes.push(m);return m}function cyl(name,x,y,z,r,h,mat,walk=false){const m=new THREE.Mesh(new THREE.CylinderGeometry(r,r,h,20),mat);m.name=name;m.position.set(x,y,z);m.castShadow=!mobile;m.receiveShadow=true;world.add(m);if(walk)walkMeshes.push(m);return m}function tree(x,z,s=1){cyl('tree-trunk',x,.9*s,z,.28*s,1.8*s,mats.wood);const c=new THREE.Mesh(new THREE.ConeGeometry(1.2*s,2.5*s,10),mats.leaf);c.position.set(x,2.45*s,z);c.castShadow=!mobile;world.add(c)}function house(x,z,scale=1,rot=0){const g=new THREE.Group(),base=new THREE.Mesh(new THREE.BoxGeometry(4.5*scale,2.7*scale,3.8*scale),mats.plaster);base.position.y=1.35*scale;base.castShadow=!mobile;base.receiveShadow=true;g.add(base);const roof=new THREE.Mesh(new THREE.ConeGeometry(3.6*scale,2.2*scale,4),mats.roof);roof.position.y=3.55*scale;roof.rotation.y=Math.PI/4;roof.castShadow=!mobile;g.add(roof);const door=new THREE.Mesh(new THREE.BoxGeometry(.8*scale,1.6*scale,.16*scale),mats.wood);door.position.set(0,.8*scale,2*scale);g.add(door);g.position.set(x,0,z);g.rotation.y=rot;world.add(g)}function arch(x,y,z,s=1,rot=0){box('ruin-pillar',x-1.65*s,y+1.7*s,z,.65*s,3.4*s,.75*s,mats.stone,false,rot);box('ruin-pillar',x+1.65*s,y+1.7*s,z,.65*s,3.4*s,.75*s,mats.stone,false,rot);box('ruin-lintel',x,y+3.35*s,z,3.95*s,.6*s,.8*s,mats.stone,false,rot)}
function buildWorld(){progress('Laying out meadow, village, bridge, hillside and ruins…',18);box('meadow',0,-.45,18,30,.9,34,mats.grass,true);box('village-ground',0,-.35,-10,32,.7,24,mats.grass2,true);box('north-bank',0,-.35,-31,32,.7,14,mats.grass,true);box('route-path',0,.03,15,5,.12,30,mats.path,true);box('village-path',0,.03,-8,5.5,.12,18,mats.path,true);for(const [x,z,s] of [[-11,26,1.1],[11,25,.9],[-12,14,.85],[12,8,1],[9,31,.75],[-9,33,.8]])tree(x,z,s);for(let i=0;i<6;i++){box('fence',-4.5,.55,28-i*4,.18,1.1,3.1,mats.wood);box('fence',4.5,.55,28-i*4,.18,1.1,3.1,mats.wood)}house(-9,-6,.9,.08);house(9,-7,1,-.12);house(-10,-15,.82,-.05);house(10,-16,.92,.12);house(-8,-22,.74,.06);house(8,-23,.78,-.08);box('village-square',0,.06,-15,11,.14,7,mats.path,true);cyl('well',-4,.65,-15,1.1,1.1,mats.stone);cyl('well-cap',-4,1.25,-15,1.35,.18,mats.stone2);box('river',0,-.82,-40,42,.18,12,mats.water,false);box('bridge-deck',0,.42,-40,6,1.0,13,mats.wood,true);for(const x of [-3.15,3.15]){for(let z=-45;z<=-35;z+=2)box('bridge-post',x,1.28,z,.22,1.7,.22,mats.wood);box('bridge-rail',x,1.75,-40,.18,.2,10.5,mats.wood)}box('far-bank',0,-.2,-51,34,.8,12,mats.grass,true);box('approach-path',0,.25,-51,5,.14,12,mats.path,true);
 // Correct orientation: route moves toward decreasing Z while climbing.
 const angle=.14;box('hill-ramp-1',0,.65,-61,12,1.0,12,mats.grass,true,0,angle);box('hill-ramp-2',0,2.25,-72,12,1.0,12,mats.grass2,true,0,angle);box('hill-ramp-3',0,3.85,-83,12,1.0,12,mats.grass,true,0,angle);box('hill-top',0,5.15,-94,30,1,15,mats.grass,true);box('hill-path',0,5.72,-94,5,.14,14,mats.path,true);box('side-step-a',8,5.8,-88,4,.7,4,mats.stone,true);box('side-step-b',10.5,6.6,-91,4,.7,4,mats.stone,true);box('lookout',12,7.4,-96,7,.8,7,mats.stone,true);box('ruins-ground',0,5.15,-110,34,1,22,mats.grass2,true);box('ruins-path',0,5.72,-108,5,.14,20,mats.path,true);arch(0,5.65,-103,1.05);arch(-8,5.65,-110,.85,.08);arch(8,5.65,-114,.75,-.06);box('ruin-platform-a',-5,6.15,-108,5,1,5,mats.stone,true);box('ruin-platform-b',5,6.55,-113,5,1.8,5,mats.stone,true);box('ruin-platform-c',0,7.05,-118,6,2.5,5,mats.stone,true);box('ruins-mainwalk',0,5.75,-116,7,.2,14,mats.stone2,true);box('shrine-ground',0,5.15,-133,30,1,24,mats.grass,true);box('shrine-court',0,5.72,-132,17,.18,15,mats.stone2,true);box('shrine-step',0,5.95,-138,9,.6,5,mats.stone,true);for(const x of [-8,8]){cyl('shrine-column',x,7.5,-132,.55,4,mats.stone);cyl('shrine-cap',x,9.55,-132,.8,.2,mats.stone2)}arch(0,5.7,-144,1.25);for(const [x,z,s] of [[-12,-30,.8],[12,-29,.9],[-13,-53,.8],[13,-54,.9],[-13,-91,.9],[13,-101,.8],[-12,-126,.85],[12,-139,.9]])tree(x,z,s);world.updateMatrixWorld(true);worldBounds=new THREE.Box3().setFromObject(world);window.__worldSource='pass9-procedural-world1-greybox';window.__worldRoute='meadow-village-river-bridge-hillside-ruins-portal-shrine';window.__enemyCount=0;window.__continuousLand=true;return{spawn:{x:0,y:.04,z:31},tube:{x:0,y:6.28,z:-138}}}
const heightCache=new Map();function hkey(x,z){return`${Math.round(x*8)},${Math.round(z*8)}`}function groundAt(x,z,cache=true){const key=hkey(x,z);if(cache&&heightCache.has(key))return heightCache.get(key);const top=worldBounds.max.y+16;ray.set(new THREE.Vector3(x,top,z),down);ray.far=(top-worldBounds.min.y)+28;const hits=ray.intersectObjects(walkMeshes,false);const y=hits.length?hits[0].point.y:null;if(cache)heightCache.set(key,y);return y}
let bro,heroRoot,tube,sites;const player={x:0,y:0,z:0,yaw:Math.PI,vy:0,speed:0,grounded:true,jumpCount:0,holdJumpTimer:0};const moveKeys={up:false,down:false,left:false,right:false,run:false,jump:false,jumpQueued:false},analogMove={x:0,y:0,active:false,pointerId:null};const STEP_HEIGHT=.58,GRAVITY=8.25,JUMP_V=5.05,DOUBLE_JUMP_V=4.65,HOLD_BOOST_V=3.95,HOLD_BOOST_INTERVAL=.22;
async function createPlayer(){progress('Restoring Crystal Library v2 movement and Gamer Bro rig…',45);bro=createGamerBro(THREE,renderer,{colorway,detail:mobile?'low':'high',height:2.62});heroRoot=bro.root;bro.all.forEach(m=>{m.layers.enable(0);m.layers.enable(1)});bro.setRoomLight(1);scene.add(heroRoot);resetPlayer();window.__characterReady=id;window.__playerPosition=player;window.__movementBaseline='crystal-library-v2';window.__cameraBaseline='crystal-library-v2'}function resetPlayer(){player.x=sites.spawn.x;player.z=sites.spawn.z;player.y=(groundAt(player.x,player.z,false)??sites.spawn.y)+.02;player.yaw=Math.PI;player.vy=0;player.speed=0;player.grounded=true;player.jumpCount=0;if(bro){bro.setHome(player.x,player.z,player.yaw);heroRoot.position.set(player.x,player.y,player.z)}}function createTube(){progress('Installing canonical Crystal Library v2 tube at shrine…',67);tube=createCrystalLibraryTubeV2({renderer,scene,camera,bro,heroRoot,mobile,onComplete:()=>{objective.textContent='WORLD 1 ROUTE COMPLETE · PASS 9';retry.classList.add('show');toast('ROUTE COMPLETE',2200)}});tube.root.position.set(sites.tube.x,sites.tube.y-.42,sites.tube.z);window.__tubeComponent='crystal-library-v2-tube';window.__tubePosition={...sites.tube};window.__portalRequiresEnemies=false}
const target=new THREE.Vector3(),targetLook=new THREE.Vector3();let yaw=.06,pitch=.34,distance=13.4,followDistance=13.4,recenterClock=0,dragging=false,lastX=0,lastY=0;function cameraDesired(dt){const portalActive=tube&&tube.state!=='idle';if(portalActive){targetLook.set(tube.root.position.x,tube.root.position.y+2.9,tube.root.position.z);target.lerp(targetLook,.085);yaw=THREE.MathUtils.damp(yaw,.06,3,dt);pitch=THREE.MathUtils.damp(pitch,.10,3,dt);distance=THREE.MathUtils.damp(distance,12.2,3.4,dt)}else{targetLook.set(player.x,player.y+1.45,player.z);target.lerp(targetLook,1-Math.exp(-8*dt));if(dragging||analogMove.active)recenterClock=0;else recenterClock+=dt;if(!dragging&&!analogMove.active&&recenterClock>.85){const behind=player.yaw+Math.PI,dy=Math.atan2(Math.sin(behind-yaw),Math.cos(behind-yaw));yaw+=dy*(1-Math.exp(-1.75*dt));pitch=THREE.MathUtils.damp(pitch,.34,2.5,dt)}distance=THREE.MathUtils.damp(distance,followDistance,3.8,dt)}const cp=Math.cos(pitch),desired=new THREE.Vector3(target.x+Math.sin(yaw)*cp*distance,target.y+Math.sin(pitch)*distance+1.05,target.z+Math.cos(yaw)*cp*distance);const dir=desired.clone().sub(target),len=dir.length();if(len>0&&walkMeshes.length){ray.set(target,dir.normalize());ray.far=len;const hit=ray.intersectObjects(walkMeshes,false)[0];if(hit&&hit.distance<len-.2)desired.copy(target).addScaledVector(dir,Math.max(2.9,hit.distance-.38))}camera.position.lerp(desired,1-Math.exp(-16*dt));camera.lookAt(target)}
let actionCooldown=0,actionRing=null;function useAction(){if(!bro||!tube||tube.state!=='idle'||actionCooldown>0)return;actionCooldown=.55;actionBtn.classList.add('active');setTimeout(()=>actionBtn.classList.remove('active'),120);if(actionRing)scene.remove(actionRing);actionRing=new THREE.Mesh(new THREE.RingGeometry(.5,1.05,48),new THREE.MeshBasicMaterial({color:0xa768ff,transparent:true,opacity:.78,side:THREE.DoubleSide,depthWrite:false,blending:THREE.AdditiveBlending}));actionRing.rotation.x=-Math.PI/2;actionRing.position.set(player.x,player.y+.08,player.z);scene.add(actionRing);actionRing.userData.age=0;window.__actionCount=(window.__actionCount||0)+1;window.dispatchEvent(new CustomEvent('gamer-bros-action',{detail:{hero:id,x:player.x,y:player.y,z:player.z}}));toast('POWER / ACTION')}function updateAction(dt){actionCooldown=Math.max(0,actionCooldown-dt);if(!actionRing)return;actionRing.userData.age+=dt;const p=Math.min(1,actionRing.userData.age/.52);actionRing.scale.setScalar(1+p*4);actionRing.material.opacity=.78*(1-p);if(p>=1){scene.remove(actionRing);actionRing.geometry.dispose();actionRing.material.dispose();actionRing=null}}
function updatePlayer(dt){if(!bro||!tube||tube.state!=='idle')return;const kx=(moveKeys.right?1:0)-(moveKeys.left?1:0),ky=(moveKeys.up?1:0)-(moveKeys.down?1:0);let sx=analogMove.active?analogMove.x:kx,sy=analogMove.active?-analogMove.y:ky,len=Math.hypot(sx,sy);if(len<.075){sx=0;sy=0;len=0}const inputMag=Math.min(1,len);if(len>1){sx/=len;sy/=len}let dx=0,dz=0;if(len>0){const fx=-Math.sin(yaw),fz=-Math.cos(yaw),rx=Math.cos(yaw),rz=-Math.sin(yaw);dx=fx*sy+rx*sx;dz=fz*sy+rz*sx;const dl=Math.hypot(dx,dz)||1;dx/=dl;dz/=dl;const ty=Math.atan2(dx,dz),diff=Math.atan2(Math.sin(ty-player.yaw),Math.cos(ty-player.yaw));player.yaw+=diff*(1-Math.exp(-8.5*dt))}const speedScale=THREE.MathUtils.smoothstep(inputMag,.075,.88),targetSpeed=len>0?(moveKeys.run?6.35:3.75)*speedScale:0;player.speed=THREE.MathUtils.damp(player.speed,targetSpeed,targetSpeed>player.speed?7.5:11,dt);const currentH=groundAt(player.x,player.z),nx=player.x+dx*player.speed*dt,nz=player.z+dz*player.speed*dt,nextH=groundAt(nx,nz);if(nextH!==null&&(currentH===null||nextH-currentH<=STEP_HEIGHT)){player.x=nx;player.z=nz}if(moveKeys.jumpQueued){if(player.grounded){player.vy=JUMP_V;player.grounded=false;player.jumpCount=1;player.holdJumpTimer=HOLD_BOOST_INTERVAL}else if(player.jumpCount<2){player.vy=Math.max(player.vy,DOUBLE_JUMP_V);player.jumpCount=2;player.holdJumpTimer=HOLD_BOOST_INTERVAL}moveKeys.jumpQueued=false}if(moveKeys.jump&&!player.grounded){player.holdJumpTimer-=dt;if(player.holdJumpTimer<=0){player.vy=Math.max(player.vy,HOLD_BOOST_V);player.holdJumpTimer=HOLD_BOOST_INTERVAL}}player.vy-=GRAVITY*dt;player.y+=player.vy*dt;const ground=groundAt(player.x,player.z);if(ground!==null&&player.vy<=0&&player.y<=ground+.035){player.y=ground+.02;player.vy=0;player.grounded=true;player.jumpCount=0}else if(ground===null||player.y>ground+.08)player.grounded=false;if(player.y<worldBounds.min.y-6){resetPlayer();toast('BACK AT THE MEADOW')}const motion=player.speed<.14?'idle':(player.speed<3.7?'walk':'run');bro.setMotion(motion);bro.setHome(player.x,player.z,player.yaw);heroRoot.position.y=player.y;window.__heroMotion=motion;const d=Math.hypot(player.x-sites.tube.x,player.z-sites.tube.z);if(d<tube.entryRadius&&player.grounded&&tube.prewarmed){player.speed=0;bro.setMotion('idle');tube.trigger();objective.textContent='CRYSTAL LIBRARY V2 · TRANSMISSION IN PROGRESS'}}
const keyMap={KeyW:'up',ArrowUp:'up',KeyS:'down',ArrowDown:'down',KeyA:'left',ArrowLeft:'left',KeyD:'right',ArrowRight:'right'};addEventListener('keydown',e=>{if(keyMap[e.code])moveKeys[keyMap[e.code]]=true;if(e.code==='ShiftLeft'||e.code==='ShiftRight')moveKeys.run=true;if(e.code==='Space'){e.preventDefault();if(!moveKeys.jump)moveKeys.jumpQueued=true;moveKeys.jump=true}if(['KeyE','KeyX','KeyJ','KeyK'].includes(e.code))useAction()});addEventListener('keyup',e=>{if(keyMap[e.code])moveKeys[keyMap[e.code]]=false;if(e.code==='ShiftLeft'||e.code==='ShiftRight')moveKeys.run=false;if(e.code==='Space')moveKeys.jump=false});function setStick(x,y){const r=pad.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,lim=r.width*.34;let dx=x-cx,dy=y-cy,d=Math.hypot(dx,dy);if(d>lim){dx=dx/d*lim;dy=dy/d*lim}let nx=dx/lim,ny=dy/lim;if(Math.hypot(nx,ny)<.035){nx=ny=0}analogMove.x=nx;analogMove.y=ny;analogMove.active=true;stick.style.transform=`translate(${nx*lim}px,${ny*lim}px)`}pad.addEventListener('pointerdown',e=>{analogMove.pointerId=e.pointerId;pad.setPointerCapture(e.pointerId);setStick(e.clientX,e.clientY)});pad.addEventListener('pointermove',e=>{if(e.pointerId===analogMove.pointerId)setStick(e.clientX,e.clientY)});function stopStick(e){if(e.pointerId!==analogMove.pointerId)return;analogMove.active=false;analogMove.pointerId=null;analogMove.x=analogMove.y=0;stick.style.transform='translate(0,0)'}pad.addEventListener('pointerup',stopStick);pad.addEventListener('pointercancel',stopStick);function hold(btn,key){btn.addEventListener('pointerdown',e=>{e.preventDefault();btn.setPointerCapture(e.pointerId);moveKeys[key]=true;if(key==='jump')moveKeys.jumpQueued=true;btn.classList.add('active')});const off=()=>{moveKeys[key]=false;btn.classList.remove('active')};btn.addEventListener('pointerup',off);btn.addEventListener('pointercancel',off)}hold(jumpBtn,'jump');hold(runBtn,'run');actionBtn.addEventListener('pointerdown',e=>{e.preventDefault();useAction()});canvas.addEventListener('pointerdown',e=>{if(e.target!==canvas)return;dragging=true;lastX=e.clientX;lastY=e.clientY;canvas.setPointerCapture?.(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(!dragging)return;const dx=e.clientX-lastX,dy=e.clientY-lastY;lastX=e.clientX;lastY=e.clientY;yaw-=dx*.00245;pitch=THREE.MathUtils.clamp(pitch+dy*.0019,-.08,.78);recenterClock=0});canvas.addEventListener('pointerup',()=>dragging=false);canvas.addEventListener('pointercancel',()=>dragging=false);canvas.addEventListener('wheel',e=>{followDistance=THREE.MathUtils.clamp(followDistance+e.deltaY*.008,7.2,18);e.preventDefault()},{passive:false});retry.onclick=()=>{tube.reset();resetPlayer();retry.classList.remove('show');objective.textContent='MEADOW → VILLAGE → BRIDGE → HILLSIDE → RUINS → PORTAL';toast('ROUTE RESTARTED')};
function resize(){const w=innerWidth,h=innerHeight;renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix()}addEventListener('resize',resize);resize();const clock=new THREE.Clock();let elapsed=0;function loop(){requestAnimationFrame(loop);const dt=Math.min(.033,clock.getDelta());elapsed+=dt;updatePlayer(dt);updateAction(dt);tube?.update(elapsed,dt);cameraDesired(dt);renderer.render(scene,camera)}
(async()=>{try{sites=buildWorld();worldBounds=new THREE.Box3().setFromObject(world);await createPlayer();createTube();progress('Prewarming tube and shaders…',82);await tube.prewarm();progress('World 1 traversal ready',100);window.__pass='pass9-v1';window.__world1Ready=true;window.__actionReady=true;setTimeout(()=>boot.classList.add('hide'),180);loop()}catch(err){console.error(err);errorEl.textContent=err?.stack||err?.message||String(err);fatal.classList.add('show')}})();